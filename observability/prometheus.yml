global:
  scrape_interval: 5s
  evaluation_interval: 5s

scrape_configs:
  # Local dev: chord node running natively on the host (default port 5005).
  - job_name: "chord-nodes-local"
    metrics_path: "/prom_metrics"
    static_configs:
      - targets:
          - "host.docker.internal:5005"
          - "host.docker.internal:5006"
          - "host.docker.internal:5007"
        labels:
          env: "local"

  # Docker-compose deployment (containerized nodes). Uncomment when using
  # docker compose to run chord nodes.
  # - job_name: "chord-nodes"
  #   metrics_path: "/prom_metrics"
  #   static_configs:
  #     - targets:
  #         - "chord-node-1:5000"
  #         - "chord-node-2:5000"
  #         - "chord-node-3:5000"
  #       labels:
  #         env: "docker"
